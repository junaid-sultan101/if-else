#include<iostream>
using namespace std;
main()
{
	int age;
	cout<<" Enter age in number : ";
	cin>>age;
	if(age>=1 && (age>=9))
	{
	if( (age>=18)  )
	{
		cout<<"You are eligible for voat ...";
	}

	else
	cout<<"You are not eligible for voat";
		
	}
	else
	cout<<"invalid input";

}
